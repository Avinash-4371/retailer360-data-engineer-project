retailer360_dbt:
  target: dev
  outputs:
   dev:
    type: bigquery
    method: oauth
    project: "{{ env_var('PROJECT_ID') }}"
    dataset: retailer360_base
    location: us-central1
    threads: 4
    timeout_seconds: 300
    priority: interactive